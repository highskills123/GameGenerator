# Ollama settings
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5-coder:7b

# ngrok tunnel (optional — only needed when using --ngrok flag)
# Get a free authtoken at https://dashboard.ngrok.com/get-started/your-authtoken
# Without a token ngrok still works but sessions are limited to 2 hours.
# NGROK_AUTHTOKEN=your_ngrok_authtoken_here

# ngrok static/reserved domain (optional)
# If you have a reserved domain (e.g. from ngrok dashboard → Domains),
# set it here so the URL is always the same every time you start the server.
# NGROK_DOMAIN=costless-dorthy-unmeanderingly.ngrok-free.dev

# Optional: For Discord bot
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Optional: API server URL (if running on different host)
AIBASE_API_URL=http://localhost:5000/api/translate
